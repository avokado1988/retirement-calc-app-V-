# 🚀 הוראות פתיחת סשן חדש

## קרא קודם כל

**⚠️ חובה לקרוא: [`handoff.md`](./handoff.md)**

הקובץ הזה מכיל את כל המידע שצריך כדי להבין את הפרויקט:
- מה התוכנה עושה
- איך הקוד מארגן
- איך מעדכנים קוד
- מה יודע לעבוד ומה לא

---

## 1. בדוק את הענף הנכון

```bash
git branch
git checkout claude/reverse-mortgage-track4
```

יש להיות על ענף `claude/reverse-mortgage-track4` — כל העבודה שם.

---

## 2. התקן dependencies

```bash
pip install streamlit pandas
```

---

## 3. הרץ את האפליקציה

```bash
streamlit run app.py
```

יפתח ב-`http://localhost:8501`

---

## 4. בדוק את התכונות החדשות (משכנתה הפוכה)

1. **מלא את הקלטים** בצד שמאל (או טען `user_defaults.json`)
2. **עבור ל-Track 4** (מסלול 4 — שכירות)
3. **בדוק את טבלת הסיכון:**
   - צריכה להופיע **לפני** הכפתור "הפעל משכנתה הפוכה"
   - מראה גילאים שבהם החיסכון מתרוקן ל-₪80K, ₪40K, ₪20K
   - מראה גרעון מצטבר מכל גיל עד גיל הבדיקה
4. **הפעל משכנתה הפוכה:**
   - סמן את הכפתור
   - בחר סכום הלוואה וריבית
   - הרץ סימולציה
   - בדוק טבלת נתונים — צריכות להיות עמודות חדשות של משכנתה

---

## 5. אם יש בעיות

**קודם כל קרא את:** [`handoff.md` — סעיף 8 (Known Issues)](./handoff.md#8-known-issues--crucial-context)

אחרי שקראת את הבעיות הידועות:
- בדוק אם הטעות שלך רשומה שם
- אם לא — חפש בקוד התשובה או צור issue חדש

---

## 6. איך להמשיך בעבודה

**אתה בסביבת עבודה בענף `claude/reverse-mortgage-track4`**

עדכנת קוד?
```bash
git add .
git commit -m "תיאור השינוי"
git push origin claude/reverse-mortgage-track4
```

---

## 📋 Handoff Document Structure

כדי למצוא מידע מסוים ב-`handoff.md`, קרא:

| מה אתה צריך | קטע |
|---|---|
| הבנה כללית של התוכנה | סעיף 1 (App Objectives) |
| איך הקוד מארגן | סעיף 2 (Architecture) |
| מה הנתונים והמודלים | סעיף 3 (Data Models) |
| איך כותבים קוד | סעיף 4 (Coding Standards) |
| איך מרים את התוכנה | סעיף 5 (Environment) |
| איך משמרים / דוחפים | סעיף 6 (Branching) |
| איך השאר לעבודה | סעיף 7 (Current State) |
| בעיות ידועות | סעיף 8 (Known Issues) |

---

## שאלות נפוצות

**Q: איך אפעיל רק תיקון 190 בלי משכנתה?**  
A: בתחתית Track 4, בחר ריבית / גיל / סכום = 0

**Q: הטבלה לא מציגה משכנתה — למה?**  
A: בדוק ש-`rm_enabled = True` בקלטים, והרץ סימולציה

**Q: איך אוכל לשתף את התרחיש שלי?**  
A: ראה את ה-URL בדפדפן — שם כל הפרמטרים; שתוף את הקישור

**Q: איך אני מוסיף מסלול חמישי?**  
A: ראה `handoff.md` סעיף 8 — "How to extend"

---

**📌 זכור:** קודם קרא את `handoff.md`!
