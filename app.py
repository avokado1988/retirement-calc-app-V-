import streamlit as st
import inputs

# הגדרות בסיסיות לעמוד
st.set_page_config(page_title="מחשבון פרישה חכם", page_icon="📊", layout="wide")

# כותרת ראשית במסך המרכזי
st.title("📊 מחשבון פרישה חכם (גרסת פייתון)")
st.markdown("ברוכים הבאים למערכת תכנון הפרישה. אנא הזינו את הנתונים בתפריט הצד השמאלי.")
st.divider()

# קריאה לחבילת הקלטים שלנו (זה מה שמייצר את תפריט הצד!)
user_inputs = inputs.render_all_sidebar_inputs()

# תצוגה זמנית: מראה למשתמש שהנתונים נקלטים בהצלחה
st.write("### 🛠️ מבט מתחת למכסה המנוע (זמני לבדיקה):")
st.write("אלו הנתונים שהמערכת קולטת כרגע מתפריט הצד, מוכנים להישלח למנוע הסימולציה שייבנה בשלב הבא:")
st.json(user_inputs)
