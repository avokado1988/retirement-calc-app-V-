# מסמך אפיון פרמטרים — מחשבון פרישה אקטוארי
> **שימוש פנימי בלבד.** עדכון אחרון: 2026-05-30

---

## קטגוריה א — זמנים ומחזור הסימולציה

### `start_age` — גיל התחלת סימולציה

| שדה | ערך |
|---|---|
| **סוג** | מספר עשרוני (שנים) |
| **יחידה** | שנים (למשל 65.5 = 65 שנה ו-6 חודשים) |
| **ברירת מחדל** | 67.0 |
| **מטרה** | קובע את נקודת ה-t=0 של הסימולציה — החודש הראשון בלולאה |
| **הצמדה** | לא רלוונטי — אינו גדל עם הזמן |
| **התנהגות לפני פרישה** | כל החודשים עד `retirement_age` הם "שלב הצבירה" — אין משיכות במסלולים 1–3 |
| **התנהגות אחרי פרישה** | הסימולציה ממשיכה עד גיל 105 |
| **אפיון טכני למתכנתים** | `current_age = start_age + (m / 12.0)` בלולאה. `total_months = int((105 - start_age) * 12) + 1`. מגדיר גם את נקודת הבסיס של `inflation_factor = 1.0` |
| **סטטוס** | ✓ |

---

### `retirement_age` — גיל פרישה

| שדה | ערך |
|---|---|
| **סוג** | מספר עשרוני (שנים) |
| **יחידה** | שנים |
| **ברירת מחדל** | 67.0 |
| **מטרה** | מגדיר מתי מתחילים תשלומי קצבה, ביטוח לאומי, ומשיכות מהתיק |
| **הצמדה** | לא רלוונטי |
| **התנהגות לפני פרישה** | משיכות אפסיות במסלולים 1–3. ב"ל = 0. קצבה = 0 |
| **התנהגות אחרי פרישה** | מפעיל תשלומי ב"ל, קצבה, ומשיכות. מפעיל `retirement_inflation_factor` |
| **אפיון טכני למתכנתים** | `if current_age < retirement_age: net_needed_190 = 0`. `ni_indexed = ni_base * inflation_factor if current_age >= retirement_age else 0.0`. `retirement_inflation_factor` מתחיל לרוץ רק כש-`current_age > retirement_age` |
| **סטטוס** | ✓ |

---

### `check_age` — גיל יעד לבדיקה

| שדה | ערך |
|---|---|
| **סוג** | מספר עשרוני (שנים) |
| **יחידה** | שנים |
| **ברירת מחדל** | 87.0 |
| **מטרה** | קובע עד לאיזה גיל מוצגים נתונים בגרפים ובדוחות הראשיים. אינו מגביל את הסימולציה עצמה |
| **הצמדה** | לא רלוונטי |
| **התנהגות** | הסימולציה תמיד רצה עד גיל 105. `df` (המוצג) נחתך ב-`check_age`. `df_full` כולל עד 105 |
| **אפיון טכני למתכנתים** | `return {"df": df_full[df_full["גיל"] <= check_age], "df_full": df_full, ...}` |
| **סטטוס** | ✓ |

---

## קטגוריה ב — הוצאות

### `current_expenses` — הוצאה חודשית בסיס

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **יחידה** | ₪/חודש |
| **ברירת מחדל** | 11,000 |
| **מטרה** | הוצאת מחיה חודשית של הלקוח בערכי היום — הבסיס לכל חישוב shortfall |
| **הצמדה** | כן — מוצמד מגיל ההתחלה דרך `inflation_factor` |
| **ממתי מוצמד** | מגיל `start_age` (חודש 0) |
| **למה מוצמד** | הוצאות מחיה עולות עם האינפלציה לאורך כל שנות הפרישה |
| **אפיון טכני למתכנתים** | `curr_base_exp = base_monthly_expense` (ערך גולמי). `nominal_expense = curr_base_exp * inflation_factor`. ההצמדה מופעלת דרך הכפל ב-`inflation_factor` ולא ע"י שינוי הבסיס |
| **סטטוס** | ✓ |

---

### `expected_inflation` — אינפלציה שנתית בסיסית

| שדה | ערך |
|---|---|
| **סוג** | אחוז (מאוחסן כשבר עשרוני) |
| **יחידה** | % לשנה |
| **ברירת מחדל** | 2.3% |
| **מטרה** | קצב עליית המחירים הכללי — מגדיר את קצב גדילת `inflation_factor` |
| **הצמדה** | הוא עצמו הפרמטר המגדיר את ההצמדה — אינו מוצמד |
| **אפיון טכני למתכנתים** | `i_monthly = (1 + current_ann_inf) ** (1/12) - 1`. `inflation_factor *= (1 + i_monthly)` בכל חודש (m > 0). משמש גם להצמדת בסיס במסלולי 25% ריאלי |
| **סטטוס** | ✓ |

---

### `age_75_85_increase` — תוספת אינפלציה גיל 75–85

| שדה | ערך |
|---|---|
| **סוג** | אחוז (מאוחסן כשבר עשרוני) |
| **יחידה** | % נוסף לשנה |
| **ברירת מחדל** | 0.5% |
| **מטרה** | מייצג עלייה בהוצאות בגיל מתקדם (בריאות, נסיעות, שירותים) — מעבר לאינפלציה הבסיסית |
| **הצמדה** | הוא עצמו הפרמטר — אינו מוצמד |
| **התנהגות** | מתווסף ל-`current_ann_inf` בטווח גיל 75–84.99. ההשפעה מצטברת ב-`inflation_factor` |
| **אפיון טכני למתכנתים** | `elif current_age >= 75.0: current_ann_inf += age_75_85_increase`. מגביר את קצב `inflation_factor` בשלב זה בלבד |
| **סטטוס** | ✓ |

---

### `age_85_plus_increase` — תוספת אינפלציה מגיל 85+

| שדה | ערך |
|---|---|
| **סוג** | אחוז (מאוחסן כשבר עשרוני) |
| **יחידה** | % נוסף לשנה |
| **ברירת מחדל** | 1.5% |
| **מטרה** | מייצג עלייה חדה בהוצאות בגיל מופלג — שירותי בריאות, סיעוד, שירותים מיוחדים |
| **הצמדה** | הוא עצמו הפרמטר — אינו מוצמד |
| **התנהגות** | מתווסף ל-`current_ann_inf` מגיל 85 ומעלה. מוחלף על תוספת 75–85 (לא מצטבר איתה) |
| **אפיון טכני למתכנתים** | `if current_age >= 85.0: current_ann_inf += age_85_plus_increase`. בדיקת 85 לפני בדיקת 75 — elif מבטיח שרק אחד מופעל |
| **סטטוס** | ✓ |

---

### `caregiver_cost` — עלות מטפלת סיעודית

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **יחידה** | ₪/חודש (ערך בסיס בערכי היום) |
| **ברירת מחדל** | 3,500 |
| **מטרה** | הוצאה נוספת חודשית מגיל 85 — מייצגת עלות טיפול סיעודי |
| **גיל הפעלה** | 85.0 — hardcoded בקוד המנוע ובדוח QA |
| **הצמדה** | כן — מוצמד דרך `inflation_factor` (כמו `current_expenses`) |
| **אפיון טכני למתכנתים** | `if current_age >= 85.0: curr_base_exp += caregiver_cost_base`. הסכום נוסף לפני `nominal_expense = curr_base_exp * inflation_factor` — לכן מוצמד |
| **סטטוס** | ✓ |

---

### `one_time_expense` — הוצאה חד-פעמית

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ לאירוע |
| **ברירת מחדל** | 80,000 |
| **מטרה** | מייצג הוצאות גדולות לא שגרתיות (רכב, שיפוץ, חופשה גדולה, טיפול רפואי) |
| **הצמדה** | כן — מוצמד דרך `inflation_factor` של אותו חודש |
| **התנהגות** | מתווסף ל-`curr_base_exp` בחודשים שבהם `m % freq == 0` (ו-m > 0) |
| **אפיון טכני למתכנתים** | `if freq > 0 and m > 0 and m % freq == 0: curr_base_exp += one_time_expense`. freq מחושב: `int(one_time_frequency * 12)` |
| **הערה** | הסכום מוצמד לפי ערכי-היום. אם המשתמש מזין ערך עתידי נומינלי, יש להסביר לו שהוא יוצמד |
| **סטטוס** | ✓ |

---

### `one_time_frequency` — תדירות הוצאה חד-פעמית

| שדה | ערך |
|---|---|
| **סוג** | מספר שלם (שנים) |
| **יחידה** | שנים בין אירוע לאירוע |
| **ברירת מחדל** | 8 |
| **מטרה** | קובע כל כמה שנים מתרחשת ההוצאה החד-פעמית |
| **הצמדה** | לא רלוונטי |
| **אפיון טכני למתכנתים** | `freq = int(one_time_frequency * 12)`. תנאי: `m % freq == 0 and m > 0` |
| **סטטוס** | ✓ |

---

## קטגוריה ג — הכנסות

### `national_insurance` (ב"ל) — ביטוח לאומי

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **יחידה** | ₪/חודש (ערך בסיס בערכי היום) |
| **ברירת מחדל** | 2,500 |
| **מטרה** | הכנסה חודשית קבועה מהמדינה לאחר פרישה |
| **הצמדה** | כן — מוצמד דרך `inflation_factor` מגיל `start_age` |
| **ממתי משולם** | מגיל `retirement_age` בלבד |
| **למה מוצמד מגיל ההתחלה** | ב"ל צמוד למדד — גם אם הלקוח טרם פרש, הסכום בגיל הפרישה יהיה גבוה יותר מהבסיס |
| **אפיון טכני למתכנתים** | `ni_indexed = ni_base * inflation_factor if current_age >= retirement_age else 0.0`. ה-`inflation_factor` ממשיך לצמוח גם לפני הפרישה — לכן בגיל הפרישה הסכום כבר צמוד לפי שנות ההפרש |
| **מיקום ב-`user_inputs`** | `wealth["national_insurance"]` (מוזרק מ-`incomes_ui` ב-`__init__.py`) |
| **סטטוס** | ✓ |

---

### `work_income` — הכנסה מעבודה

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **יחידה** | ₪/חודש |
| **ברירת מחדל** | 0 |
| **מטרה** | הכנסה זמנית מעבודה או גישור — מפחיתה את הצורך במשיכה מהתיק |
| **הצמדה** | לא — משולם בסכום נומינלי קבוע עד `work_end_age` |
| **התנהגות** | `curr_work_inc = work_income_static if current_age < work_end_age else 0.0` |
| **אפיון טכני למתכנתים** | `base_income = curr_work_inc + ni_indexed`. מפחית shortfall בכל 4 המסלולים |
| **מיקום ב-`user_inputs`** | `expenses["work_income"]` (מוזרק מ-`incomes_ui`) |
| **סטטוס** | ✓ |

---

### `work_end_age` — גיל הפסקת עבודה

| שדה | ערך |
|---|---|
| **סוג** | מספר עשרוני (שנים) |
| **יחידה** | שנים |
| **ברירת מחדל** | שווה ל-`retirement_age` |
| **מטרה** | קובע עד מתי ה-`work_income` פעיל |
| **הצמדה** | לא רלוונטי |
| **אפיון טכני למתכנתים** | `if current_age < work_end_age: curr_work_inc = work_income_static` |
| **מיקום ב-`user_inputs`** | `expenses["work_end_age"]` (מוזרק מ-`incomes_ui`) |
| **סטטוס** | ✓ |

---

## קטגוריה ד — הון ונדל"ן

### `net_sale` — נטו ממכירת הנכס הקיים

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ חד-פעמי |
| **ברירת מחדל** | 10,000,000 |
| **מטרה** | הכנסה ממכירת הנכס הנוכחי — כניסה ראשית להון הפנוי |
| **הצמדה** | לא — ערך נומינלי חד-פעמי בזמן הסימולציה |
| **אפיון טכני למתכנתים** | `remaining_for_gimel = net_sale + existing_savings - new_apartment_cost - kids_help - emergency_fund`. חישוב ב-`wealth.py`, מוצג בסיכום |
| **סטטוס** | ✓ |

---

### `existing_savings` — חסכונות קיימים

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ |
| **ברירת מחדל** | 440,000 |
| **מטרה** | הון נזיל קיים — נוסף ל-`net_sale` ומשמש כהון בסיס בחלק מהמסלולים |
| **הצמדה** | לא — נכנס כחד-פעמי |
| **אפיון טכני למתכנתים** | חלק מחישוב `remaining_for_gimel`. במסלול 4: `net_for_rental = existing_savings - kids_help - emergency_fund` (ללא מכירת נכס) |
| **סטטוס** | ✓ |

---

### `new_apartment_cost` — עלות דירה חדשה

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ |
| **ברירת מחדל** | 5,800,000 |
| **מטרה** | עלות רכישת הדירה החדשה — מנוכה מהון הפנוי במסלולים 1–3. בנוסף — שווי הנכס המשומר למסלול 4 |
| **הצמדה** | כערך נכנס — לא. אולם שווי הנכס בסימולציה מוצמד דרך `prop_appreciation_monthly` |
| **אפיון טכני למתכנתים** | `property_value = wealth.get("new_apartment_cost")`. `property_value *= (1 + prop_appreciation_monthly)` בכל חודש. מוצג בעמודה `"שווי נדלן"` ו-`"שווי נדלן מסלול 4"` |
| **סטטוס** | ✓ |

---

### `property_appreciation` — עליית ערך נדל"ן שנתית

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **יחידה** | % לשנה |
| **ברירת מחדל** | 2.3% |
| **מטרה** | קצב עלייה ריאלי של שווי הנכס לאורך הסימולציה |
| **הצמדה** | הוא עצמו הפרמטר — אינו מוצמד |
| **אפיון טכני למתכנתים** | `prop_appreciation_monthly = (1 + property_appreciation) ** (1/12) - 1`. `property_value *= (1 + prop_appreciation_monthly)` בכל חודש (כולל לפני פרישה) |
| **סטטוס** | ✓ |

---

### `kids_help` — עזרה לילדים

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ חד-פעמי |
| **ברירת מחדל** | 1,000,000 |
| **מטרה** | סכום המנוכה מהון הפנוי — העברה בין-דורית / מתנה לילדים בעת הפרישה |
| **הצמדה** | לא — מנוכה חד-פעמית |
| **אפיון טכני למתכנתים** | חלק מ-`remaining_for_gimel`. גם במסלול 4: `net_for_rental = existing_savings - kids_help - emergency_fund` |
| **סטטוס** | ✓ |

---

### `emergency_fund` — קרן חירום

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) |
| **יחידה** | ₪ |
| **ברירת מחדל** | 300,000 |
| **מטרה** | כסף נזיל שאינו נכנס לסימולציה — מנוכה מהון הפנוי ונשאר בצד כרזרבה |
| **הצמדה** | לא — מנוכה חד-פעמית מהבסיס |
| **אפיון טכני למתכנתים** | חלק מ-`remaining_for_gimel`. גם ב-`net_for_rental`. לא גדל בתוך הסימולציה |
| **סטטוס** | ✓ |

---

## קטגוריה ה — מסלול 1: תיקון 190

### `desired_pension` — קצבה רצויה

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **יחידה** | ₪/חודש (ערך נומינלי בגיל הפרישה) |
| **ברירת מחדל** | 5,000 |
| **מטרה** | הקצבה החודשית שהלקוח רוצה לקבל — קובע כמה הון ינוכה לרכישת הקצבה |
| **הצמדה** | הקצבה עצמה מוצמדת למדד לאחר הפרישה (`retirement_inflation_factor`) |
| **ממתי מוצמדת** | מגיל `retirement_age` בלבד (`current_age > retirement_age`) |
| **אפיון טכני למתכנתים** | `p_indexed = pension_base * retirement_inflation_factor if current_age >= retirement_age else 0.0`. בחודש הפרישה עצמו: `retirement_inflation_factor = 1.0` — כלומר הקצבה ביום הפרישה = הסכום המוזן |
| **סטטוס** | ✓ |

---

### `securing_years` — תקופת אבטחה (חודשי הבטחה ליורשים)

| שדה | ערך |
|---|---|
| **סוג** | מספר שלם (שנים) |
| **יחידה** | שנים |
| **ברירת מחדל** | 20 |
| **מטרה** | מספר שנות הבטחה לתשלום קצבה גם לאחר פטירת הלקוח — ליורשים |
| **הצמדה** | לא — מגדיר מספר חודשים בלבד |
| **השפעה כפולה** | (1) מגדיל `adjusted_coefficient` → מגדיל `capital_for_pension`. (2) קובע את ערך `pension_asset_value` (שווי ירושה קצבתי) |
| **אפיון טכני למתכנתים** | `adjusted_coefficient = base_coefficient + securing_years * 1.0`. `securing_months_total = securing_years * 12`. `guarantee_remaining = max(0, securing_months_total - months_since_retire)`. `pension_asset_value = guarantee_remaining * p_indexed` |
| **סטטוס** | ✓ |

---

### `base_coefficient` — מקדם המרה בסיסי

| שדה | ערך |
|---|---|
| **סוג** | מספר עשרוני (יחידות מקדם) |
| **יחידה** | ₪ הון לכל ₪ קצבה חודשית (ללא אבטחה) |
| **ברירת מחדל** | 200.0 |
| **מטרה** | מקדם אקטוארי — מבטא כמה הון נדרש לרכישת ₪ 1 קצבה חודשית ללא תקופת אבטחה |
| **אפיון טכני למתכנתים** | `adjusted_coefficient = base_coefficient + securing_years * 1.0`. מחושב ב-`amendment_190.py` ומוצג כ-metric |
| **סטטוס** | ✓ |

---

### `capital_for_pension` — הון הנדרש לרכישת קצבה

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) — **מחושב אוטומטית, אינו קלט ישיר** |
| **יחידה** | ₪ |
| **מטרה** | סכום המנוכה מהון הפנוי לרכישת הקצבה הרצויה |
| **הצמדה** | לא — חד-פעמי |
| **אפיון טכני למתכנתים** | `capital_for_pension = int(desired_pension * adjusted_coefficient)`. מנוכה: `net_for_190 = max(0, remaining_for_gimel - capital_for_pension)` |
| **סטטוס** | ✓ |

---

### `annual_return_190` / `management_fee_190` — תשואה ודמי ניהול מסלול 190

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **יחידה** | % לשנה |
| **ברירות מחדל** | תשואה: 5.0% / דמי ניהול: 0.6% |
| **מטרה** | תשואה גולמית ועלות ניהול שנתית על יתרת תיקון 190 |
| **הצמדה** | לא — פרמטרים קבועים |
| **אפיון טכני למתכנתים** | `r_monthly_190 = (1 + (annual_return_190 - management_fee_190)) ** (1/12) - 1`. תשואה נטו חודשית גיאומטרית. מוחסרת **פעם אחת בלבד** (בנוסחת r_monthly) ולא שוב בלולאה |
| **סטטוס** | ✓ |

---

## קטגוריה ו — מסלול 2: 25% מס ריאלי

### `net_for_real_pathway` — הון בסיס מסלול 2

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) — **מחושב אוטומטית** |
| **יחידה** | ₪ |
| **מטרה** | הון הנכנס למסלול 25% ריאלי — זהה ל-`remaining_for_gimel` (לפני ניכוי קצבה) |
| **הצמדה** | הון גולמי נכנס — לא מוצמד בנקודת הכניסה |
| **אפיון טכני למתכנתים** | `balance_25 = real_tax_25.get("net_for_real_pathway")`. `basis_25 = balance_25` (בסיס ראשוני). **שדה `baseline_capital`** = `balance_25` — בסיס לחישוב ratio בגיל 97 |
| **סטטוס** | ✓ |

---

### `annual_return_25` / `management_fee_25` — תשואה ודמי ניהול מסלול 2

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **ברירות מחדל** | תשואה: 5.0% / דמי ניהול: 0.6% |
| **אפיון טכני למתכנתים** | `r_monthly_25 = (1 + (annual_return_25 - management_fee_25)) ** (1/12) - 1` |
| **סטטוס** | ✓ |

---

### מנגנון מס 25% ריאלי — `basis_25`

| שדה | ערך |
|---|---|
| **סוג** | מנגנון פנימי (לא קלט משתמש) |
| **מטרה** | עוקב אחרי הבסיס (עלות מקורית מוצמדת) לצורך חישוב הרווח הריאלי בלבד |
| **הצמדה** | כן — `basis_25 *= (1 + i_monthly)` בכל חודש **לאחר** המשיכה |
| **סדר חיוני** | משיכה → עדכון בסיס → תשואה (לפי CLAUDE.md) |
| **אפיון טכני למתכנתים** | (1) `basis_25 *= (1 - pull25/balance_25)` — ניכוי יחסי בעת משיכה. (2) `basis_25 *= (1 + i_monthly)` — הצמדה. (3) `balance_25 *= (1 + r_monthly_25)` — תשואה. מס = `pull * (balance - basis)/balance * 0.25` |
| **סטטוס** | ✓ |

---

## קטגוריה ז — מסלול 3: היברידי (קצבה + 25% ריאלי)

### `net_for_hybrid` — הון בסיס מסלול 3

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) — **מחושב אוטומטית** |
| **מטרה** | זהה ל-`net_for_190` — אותו הון נכנס, אך מנוהל ב-25% ריאלי במקום 190 |
| **אפיון טכני למתכנתים** | `inputs_dict["real_tax_25"]["net_for_hybrid"] = amendment_190.get("net_for_190", 0)` — מוזרק ב-`__init__.py` |
| **סטטוס** | ✓ |

---

### `annual_return_hybrid` / `management_fee_hybrid` — תשואה ודמי ניהול מסלול 3

| שדה | ערך |
|---|---|
| **ברירות מחדל** | תשואה: 5.0% / דמי ניהול: 0.6% |
| **אפיון טכני למתכנתים** | `r_monthly_hybrid = (1 + (annual_return_hybrid - management_fee_hybrid)) ** (1/12) - 1` |
| **סטטוס** | ✓ |

---

## קטגוריה ח — מסלול 4: שכירות

### `net_for_rental` — הון נזיל מסלול 4

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪) — **מחושב אוטומטית** |
| **מטרה** | הון נזיל של הלקוח כשהנכס לא נמכר — חסכונות בלבד |
| **אפיון טכני למתכנתים** | `net_for_rental = existing_savings - kids_help - emergency_fund`. **אינו כולל** `net_sale` (הנכס לא נמכר) |
| **סטטוס** | ✓ |

---

### `rental_income_monthly` — הכנסת שכ"ד חודשית

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **ברירת מחדל** | 8,000 |
| **מטרה** | הכנסה ברוטו מהשכרת הנכס הנשמר |
| **הצמדה** | כן — גדלה לפי `rental_income_growth_rate` (גיאומטרי) |
| **אפיון טכני למתכנתים** | `rental_income_gross = rental_income_base * rental_income_factor`. `net_rental_income = rental_income_gross * (1 - rental_tax_rate)` |
| **סטטוס** | ✓ |

---

### `rental_income_growth_rate` — קצב גדילת הכנסת שכ"ד

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **ברירת מחדל** | 3.0% |
| **אפיון טכני למתכנתים** | `rental_income_growth_monthly = (1 + rental_income_growth_rate) ** (1/12) - 1`. `rental_income_factor *= (1 + rental_income_growth_monthly)` בכל חודש (m > 0) |
| **סטטוס** | ✓ |

---

### `rent_paid_monthly` — שכ"ד חודשי לתשלום (מגורי הלקוח)

| שדה | ערך |
|---|---|
| **סוג** | מטבע (₪ חודשי) |
| **ברירת מחדל** | 6,000 |
| **מטרה** | הוצאה חודשית של הלקוח על דירת מגורים (כשנכסו מושכר) |
| **הצמדה** | כן — גדלה לפי `rent_paid_growth_rate` |
| **אפיון טכני למתכנתים** | `rent_paid_indexed = rent_paid_base * rent_paid_factor`. מתווסף ל-`total_out_rental = nominal_expense + rent_paid_indexed` |
| **סטטוס** | ✓ |

---

### `rent_paid_growth_rate` — קצב גדילת תשלום שכ"ד

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **ברירת מחדל** | 3.0% |
| **אפיון טכני למתכנתים** | `rent_paid_growth_monthly = (1 + rent_paid_growth_rate) ** (1/12) - 1`. `rent_paid_factor *= (1 + rent_paid_growth_monthly)` |
| **סטטוס** | ✓ |

---

### `rental_tax_rate` — מס שכירות אפקטיבי

| שדה | ערך |
|---|---|
| **סוג** | אחוז (שבר עשרוני) |
| **יחידה** | % מהכנסת שכ"ד ברוטו |
| **ברירת מחדל** | 10% (מסלול סעיף 122 — ללא ניכוי הוצאות) |
| **מטרה** | מס על הכנסת שכירות — מנוכה מההכנסה ברוטו לפני הפחתה מהוצאות |
| **הצמדה** | לא — שיעור קבוע |
| **אפיון טכני למתכנתים** | `net_rental_income = rental_income_gross * (1 - rental_tax_rate)`. מיושם על כל חודש, כולל לפני גיל פרישה (מסלול 4 פעיל מיום 0) |
| **סטטוס** | ✓ |

---

## סיכום סטטוס — כל הפרמטרים

| # | פרמטר | קטגוריה | מוצמד | סטטוס |
|---|---|---|---|---|
| 1 | `start_age` | זמנים | לא | ✓ |
| 2 | `retirement_age` | זמנים | לא | ✓ |
| 3 | `check_age` | זמנים | לא | ✓ |
| 4 | `current_expenses` | הוצאות | כן — מ-start_age | ✓ |
| 5 | `expected_inflation` | הוצאות | הוא עצמו הצמדה | ✓ |
| 6 | `age_75_85_increase` | הוצאות | הוא עצמו הצמדה | ✓ |
| 7 | `age_85_plus_increase` | הוצאות | הוא עצמו הצמדה | ✓ |
| 8 | `caregiver_cost` | הוצאות | כן — מגיל 85 | ✓ |
| 9 | `one_time_expense` | הוצאות | כן — דרך inflation_factor | ✓ |
| 10 | `one_time_frequency` | הוצאות | לא | ✓ |
| 11 | `national_insurance` | הכנסות | כן — מ-start_age, משולם מ-retirement_age | ✓ |
| 12 | `work_income` | הכנסות | לא | ✓ |
| 13 | `work_end_age` | הכנסות | לא | ✓ |
| 14 | `net_sale` | הון | לא | ✓ |
| 15 | `existing_savings` | הון | לא | ✓ |
| 16 | `new_apartment_cost` | נדל"ן | כן — שווי גדל ע"י prop_appreciation | ✓ |
| 17 | `property_appreciation` | נדל"ן | הוא עצמו הצמדה | ✓ |
| 18 | `kids_help` | הון | לא | ✓ |
| 19 | `emergency_fund` | הון | לא | ✓ |
| 20 | `desired_pension` | מסלול 1 | כן — מ-retirement_age | ✓ |
| 21 | `securing_years` | מסלול 1 | לא | ✓ |
| 22 | `base_coefficient` | מסלול 1 | לא | ✓ |
| 23 | `capital_for_pension` | מסלול 1 | לא (מחושב) | ✓ |
| 24 | `annual_return_190` | מסלול 1 | לא | ✓ |
| 25 | `management_fee_190` | מסלול 1 | לא | ✓ |
| 26 | `annual_return_25` | מסלול 2 | לא | ✓ |
| 27 | `management_fee_25` | מסלול 2 | לא | ✓ |
| 28 | `annual_return_hybrid` | מסלול 3 | לא | ✓ |
| 29 | `management_fee_hybrid` | מסלול 3 | לא | ✓ |
| 30 | `rental_income_monthly` | מסלול 4 | כן — rental_income_growth_rate | ✓ |
| 31 | `rental_income_growth_rate` | מסלול 4 | הוא עצמו הצמדה | ✓ |
| 32 | `rent_paid_monthly` | מסלול 4 | כן — rent_paid_growth_rate | ✓ |
| 33 | `rent_paid_growth_rate` | מסלול 4 | הוא עצמו הצמדה | ✓ |
| 34 | `rental_tax_rate` | מסלול 4 | לא | ✓ |
